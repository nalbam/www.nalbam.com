# CircleCI 2.0
#
version: 2
jobs:
  publish:
    docker:
      - image: opspresso/builder
    steps:
      - checkout
      - run:
          name: Publish
          command: |
            bash ./publish.sh
